[project]
name = "bottom-browser"
version = "2.0.0"
description = "A private PyQt browser with an independent local search index and bottom-mounted controls"
requires-python = ">=3.12"
dependencies = [
    "pyinstaller>=6.22.2",
    "pyqt6>=6.11.0",
    "pyqt6-webengine>=6.11.0",
]
